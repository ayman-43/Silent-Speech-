fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0
# All VSR deps (torch, torchvision, mediapipe, av, etc.) come from
# slient-speech/.venv — run the backend with that Python interpreter.
