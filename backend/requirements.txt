# Extra packages needed on top of the slient-speech venv.
# Install with:  uv pip install -r requirements.txt
# (run from the slient-speech directory so uv finds the right venv)

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0
