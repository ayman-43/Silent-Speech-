import GestureCapture from '@/components/GestureCapture';

export const metadata = {
  title: 'Gesture Recognition — SilentSpeak AI',
  description: 'Real-time hand gesture detection powered by MediaPipe.',
};

export default function GesturePage() {
  return <GestureCapture />;
}
